# Roadmap

## Phase 0 — 当前交付

独立客户端模块、Rust 模块化单体、Native C ABI、实例模型、共享协议样本、SQLite 端口和初始实现、PostgreSQL schema、配置、Compose、CI、ADR。运行与平台验证范围见 VALIDATION。

## Phase 1 — 进行中：两个真实客户端聊天

已纵向接通 1–6 的可运行切片：Auth、社区/频道、消息、图片附件、Gateway READY/heartbeat/resume、SQLite 同步、虚拟化时间线。社区可配置屏蔽词与发言冷却。本机默认 `local:` 存储；Postgres 适配与 migration 保留给 Compose/CI。

UI 基础：独立 `Chat.Motion` 动画模块已接入页面/频道/设置切换，支持中断、减少动效及隐藏/最小化清理；调用方式和验证边界见 [动画系统](docs/ui/MOTION.md)。文字频道已收敛为缓存就绪后的消息区轻微淡入，并取消过期加载、隔离导航刷新；不改变协议和业务阶段，整进程性能对比尚未测量。

UI 开发启动：所有入口默认连接配置的局域网服务器，自动恢复或准备账号，首次无账号时注册，内网实例无社区时创建社区及默认频道并选中。首屏无账号密码表单；账号资料和服务器地址可在设置修改，服务器选择持久保存；会话失效不会静默换号。社区菜单已接通拥有者创建文字／语音频道；创建社区后选中其默认文字频道。不注入示例消息，不改变协议或阶段。

连接便捷入口：输入 `localhost` 即解析为配置的默认服务器；显式带端口的本机地址保留原义，不改变协议。

尚未完成：

- 系统凭据库（Keychain / Credential Manager）；当前 refresh 为 0600 文件
- 双窗口 GUI 验收、断网恢复与 100/1000/10000 条负载测量
- 编辑/删除 UI、权限撤销后的离线内容禁用
- 生产 S3 上传（当前本地对象目录）

不把 Phase 0 的 URL 添加视为完成 Phase 1；上线前还需真实认证、安全限流、依赖 readiness 和日志脱敏验证。

## 后续阶段

| 阶段 | 内容与边界 |
| --- | --- |
| 2 | 编辑、删除、回复、mention、reaction、通知、typing、presence；虚拟化与全局缓存预算验收。附件上传/下载与可配置大小上限已提前落地 |
| 3 | 完整角色管理、频道 override、moderation、kick/ban；基础授权早在 Phase 1 生效 |
| 4 | 语音控制面已落地（token、状态、mute/deafen 不重连、频道音质上限与用户自选档位、输入/输出设备、20 ms/3 帧本机回环检查、隔离 worker）。剩余：链接 LiveKit 客户端、按档位编码 Opus/AEC/RNNoise、把所选设备接到对端 RTP、真实双端听筒验收；不将 native crash 放进 UI |
| 5 | 三平台 capture、H.264 硬件编码、零拷贝路径优先、1080p30/60 与 2–3 帧队列 |
| 6 | 自托管生产安装器、单一业务配置衍生基础设施配置、TLS、TURN、MinIO bucket、安全默认值、迁移/备份/恢复 |
| 7 | 媒体进程协议、资源回收、重启、崩溃恢复与分发硬化；崩溃隔离不推迟到此阶段才处理 |
| 8 | 独立 Bot token、Bot REST/Gateway、签名 webhook；不允许任意插件进入 server 进程 |
| 9 | Mobile/Web 协议兼容审查，不开始移动端 UI |
| 10 | Federation、E2EE、AV1、插件、Threads 等分别立项 |

Phase 0 不承诺闲置 50–100 MiB 已达标；后续以可复现真实窗口和工作负载作为性能门槛。

UI polish（2026-09-21）：设置、认证、服务器连接和社区菜单已完成本轮表现层优化；共享选择框样式与三语服务器文案同步。工作区快捷键（跳转频道、频道/标签切换、搜索、设置）已接入现有命令。外观增加深色/浅色占位与可开关的自定义背景（模糊、亮度、高分辨率图片与视频）。浅色主题 tokens **Not implemented yet**。不新增协议或媒体能力。真实 macOS 与离屏渲染的验证边界见 [UI 记录](docs/ui/README.md#product-surface-polish-2026-09-21)。

### 侧栏与频道管理（2026-09-21）

已实现紧凑频道分组、2 px 选中行圆角和分组旁的新增按钮。所有者可添加文字/语音频道，右键打开、重命名、添加同类频道或删除。添加与重命名直接在侧栏原位输入，Enter 保存、Esc 取消，无遮罩和居中弹窗；删除在原行展开确认，失败就地显示错误，创建语音频道不会自动加入通话。管理请求走 HTTP，变更通过 Gateway 与 SQLite 同步。

验证：最终客户端 Release 构建（0 警告/错误）、本地 API / Gateway 聚焦检查、49 项客户端契约检查通过；macOS 真实窗口验证行内添加、右键原位编辑、Enter 保存和 Esc 取消。删除 API 与确认入口已验证；远端部署、PostgreSQL 实际删除和三平台窗口尚未验证。

账号栏交互（2026-09-21）：加入语音前可设置静音/耳聋，加入时沿用；悬浮显示设备名称，设备菜单复用设置偏好。点击账号打开支持用户名、显示名称和头像编辑的资料抽屉，支持 Escape/遮罩关闭与减少动效；沿用现有资料 API，不改变阶段或远端协议。

### 内存调度与产品名称（2026-09-21）

产品默认名改为 yoyoko；保留 Chat 命名空间及固定发现路径。已实现窗口状态/空闲/压力驱动的图片预算，附件资源所有权、头像有界缓存、隐藏壁纸释放和恢复加载；Phase 1 仍进行中，100/1000/10000 消息负载及三平台性能尚未验收。[策略与证据](docs/MEMORY_SCHEDULING.md)。
