# Roadmap

## Phase 0 — 当前交付

独立客户端模块、Rust 模块化单体、Native C ABI、实例模型、共享协议样本、SQLite 端口和初始实现、PostgreSQL schema、配置、Compose、CI、ADR。运行与平台验证范围见 VALIDATION。

## Phase 1 — 进行中：两个真实客户端聊天

已纵向接通 1–6 的可运行切片：Auth、社区/频道、消息、图片附件、Gateway READY/heartbeat/resume、SQLite 同步、虚拟化时间线。社区可配置屏蔽词与发言冷却。日常服务端走 `docker compose up -d --build`（Postgres + 本地对象卷）；无 Docker 或改 Rust 时仍可用 `local:` 文件库。服务端热路径已合并鉴权查询、READY 一次拉频道，并加有界 TTL 进程缓存；消息仍以 Store 为事实源，不进 Redis。

UI 基础：独立 `Chat.Motion` 动画模块已接入页面/频道/设置切换，支持中断、减少动效及隐藏/最小化清理；频道分组与社区菜单分区用 `RevealHost` 做可中断收展。调用方式和验证边界见 [动画系统](docs/ui/MOTION.md)。文字频道已收敛为缓存就绪后的消息区轻微淡入，并取消过期加载、隔离导航刷新。时间线在客户端渲染有界 Markdown、轻量 LaTeX（`$...$` / `$$...$$`，含标题/列表/表格/pmatrix）与安全 HTML 子集。明文快路径、有界解析缓存，简单公式压成 Unicode 以避免视觉树。消息协议仍为纯文本；不引入 WebView 或完整 TeX。不改变业务阶段，整进程性能对比尚未测量。

UI 开发启动：所有入口默认连接配置的局域网服务器，自动恢复或准备账号，首次无账号时注册（用户名为设置中填写的值，未填则为 User），内网实例无社区时创建社区及默认频道并选中。首屏无账号密码表单；账号资料和服务器地址可在设置修改，服务器选择持久保存；会话失效不会静默换号。社区菜单已接通拥有者创建文字／语音频道；创建社区后选中其默认文字频道。桌面入口（`start.cmd` / `dev.cmd`）打开窗口即显示本地布局假频道、假用户和假消息，方便调前端；连上实例后假频道让位给真实社区。这些数据不进入 Core / SQLite / 网络，不改变协议或阶段。

连接便捷入口：输入 `localhost` 即解析为配置的默认服务器；显式带端口的本机地址保留原义，不改变协议。

日常说话循环已接通：作者或 `MANAGE_MESSAGES` 软删除并广播 `MESSAGE_DELETE`；发送校验同频道未删除的 `reply_to`；输入走 `POST /channels/{id}/typing`，Gateway 广播无 `seq` 的 `TYPING_START`（10 秒内最多 8 次）；拉历史 403/404 会清该频道缓存。桌面删除入口对作者或社区所有者显示。

尚未完成：

- 系统凭据库（Keychain / Credential Manager）；当前 refresh 为 0600 文件
- 双窗口 GUI 验收与 100/1000/10000 条负载测量
- kick/ban 事件仍未实现（拉历史 403/404 已清该频道缓存）
- 生产 S3 上传（Compose 已起 MinIO，附件仍写 API 本地卷）

发送与弱网：本地气泡立即出现，有界 outbox（32 条）持久化未发送文字，可取消/重试；切频道不丢在途发送。Gateway 忽略 `seq ≤ cursor`，缺口先 resume 再 identify，并只刷新当前频道一页。cursor 与消息写入同一 SQLite 事务。未做双窗口断网 GUI 验收。

不把 Phase 0 的 URL 添加视为完成 Phase 1；上线前还需真实认证、安全限流、依赖 readiness 和日志脱敏验证。

## 后续阶段

| 阶段 | 内容与边界 |
| --- | --- |
| 2 | reaction、系统通知、presence；虚拟化与全局缓存预算验收。删除、同频道回复、`TYPING_START`、作者编辑、`@username` / `@everyone` / `@here` mentions、未读/@/静音与按频道草稿已落地 |
| 3 | 完整角色管理、频道 override、moderation、kick/ban；基础授权早在 Phase 1 生效 |
| 4 | 语音控制面与 LiveKit 客户端已接入隔离 worker（PlatformAudio 发布/订阅、AEC/NS/AGC、mute 不重连）。可选系统媒体会话（SMTC / Now Playing / MPRIS）仅在已加入语音且设置打开时注册，播放键映射 mute。局域网会改写 loopback RTC；worker/LiveKit 断开后有界重连；说话光圈跟 LiveKit 活跃说话人。网页可打开 `{origin}/voice/{channel_id}` 以访客身份只进该语音房（服务端限权，不是完整 Web 客户端）。剩余：双端听筒在真实窗口的对听验收、自定义 RNNoise；不将 native crash 放进 UI |
| 5 | 三平台 capture、H.264 硬件编码、零拷贝路径优先、1080p30/60 与 2–3 帧队列 |
| 6 | 自托管生产安装器、单一业务配置衍生基础设施配置、TLS、TURN、MinIO bucket、安全默认值、迁移/备份/恢复 |
| 7 | 媒体进程协议、资源回收、重启、崩溃恢复与分发硬化；崩溃隔离不推迟到此阶段才处理 |
| 8 | 独立 Bot token、Bot REST/Gateway、签名 webhook；不允许任意插件进入 server 进程 |
| 9 | Mobile/Web 协议兼容审查，不开始移动端 UI |
| 10 | Federation、E2EE、AV1、插件、Threads 等分别立项 |

Phase 0 不承诺闲置 50–100 MiB 已达标；后续以可复现真实窗口和工作负载作为性能门槛。

UI polish（2026-09-21）：设置、认证、服务器连接和社区菜单已完成本轮表现层优化；共享选择框样式与三语服务器文案同步。工作区快捷键（跳转频道、频道/标签切换、搜索、设置）已接入现有命令。外观可切换深色/浅色，并支持可开关的自定义背景（模糊、亮度、高分辨率图片与视频；视频磁盘上限 2 GiB，解码限制在约 720p）。不新增协议或媒体能力。真实 macOS 与离屏渲染的验证边界见 [UI 记录](docs/ui/README.md#product-surface-polish-2026-09-21)。

### 成员右键与私信（2026-09-21）

成员列右键可发私信、提及、复制用户名/ID；用户卡主按钮为发私信。`POST /api/v1/dms` 对同社区用户 get-or-create `kind=dm` 频道，消息复用现有频道时间线。频道列在有会话时显示「私信」分组。占位用户不能私信。不是好友系统、群私信或 presence。

### 侧栏与频道管理（2026-09-21）

频道/账号列为 260 px，给底部头像、用户名和麦克风/耳机控件留出间距。已实现紧凑频道分组、2 px 选中行圆角和分组旁的新增按钮。所有者可添加文字/语音频道。文字频道单击打开；语音频道单击加入并打开该频道的文字聊天（vc-text），右键「设置」打开设置中的设备/音质页。添加与重命名直接在侧栏原位输入，Enter 保存、Esc 取消，无遮罩和居中弹窗；删除在原行展开确认，失败就地显示错误，创建语音频道不会自动加入通话。管理请求走 HTTP，变更通过 Gateway 与 SQLite 同步。

验证：最终客户端 Release 构建（0 警告/错误）、本地 API / Gateway 聚焦检查、49 项客户端契约检查通过；macOS 真实窗口验证行内添加、右键原位编辑、Enter 保存和 Esc 取消。删除 API 与确认入口已验证；远端部署、PostgreSQL 实际删除和三平台窗口尚未验证。

账号栏交互（2026-09-21）：加入语音前可设置静音/耳聋，加入时沿用；悬浮显示设备名称，输入与输出菜单独立，直接点选设备并复用设置偏好。点击账号打开支持用户名、显示名称、头像和资料横幅编辑的资料抽屉，支持 Escape/遮罩关闭与减少动效。横幅为可选 JPEG/PNG/GIF/WebP，经 `PATCH /users/me` 的 `banner_id` / `clear_banner` 更新。

### 内存调度与产品名称（2026-09-21）

产品默认名改为 yoyoko；保留 Chat 命名空间及固定发现路径。已实现窗口状态/空闲/压力驱动的图片预算，附件资源所有权、头像有界缓存、隐藏壁纸释放和恢复加载；Phase 1 仍进行中，100/1000/10000 消息负载及三平台性能尚未验收。[策略与证据](docs/MEMORY_SCHEDULING.md)。

实例栏头像（2026-09-21）：绑定各实例账号的圆形头像，更换/移除后刷新；静态缩略图最多保留 32 张、每张不超过 64×64，沿用账户头像下载。Release 构建和本地 macOS 原生窗口中的头像更换/移除检查通过，不改变阶段或协议。

可选深度 UltraLight：设置 → 常规开关默认关闭，隐藏/最小化卸载 ShellSurface、恢复时重建；会话、Core 时间线、语音及待发送流继续持有。已验证离屏真实视图回收、语音控制面不重连以及 macOS 原生开关/恢复；硬件/RTP 连续性与三平台性能实测待验收。[边界](docs/MEMORY_SCHEDULING.md#可选深度-ultralight)。

UltraLight 恢复保护：已接入恢复失败重试与故障后暂停再次卸载，修复隐藏时误读消息。独立本地双账号 HTTP/Gateway/SQLite 实测确认后台接收、上传发送、对端下载与恢复去重；语音控制面保持连接，硬件/RTP 验收仍单列。[记录](docs/MEMORY_SCHEDULING.md#恢复保护与聊天连续性2026-09-21)。
